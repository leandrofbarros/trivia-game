import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    marginTop: 50,
    minHeight:70
  },
  title: {
    fontSize: 28,
    fontWeight: '700',
    textAlign: 'center',
  }
})
